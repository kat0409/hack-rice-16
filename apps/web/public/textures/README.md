# Textures

Reserved for optional supplementary paper/texture image assets. The primary
paper background is generated with CSS (see `src/styles/paper-texture.css`)
so nothing here is required for the app to run.
