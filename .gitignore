# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets — design-doc.md §16.1: "Commit `.env.example`, never `.env`."
# This rule was previously unenforced by .gitignore; adding it here.
.env

# Local uploaded course documents (UPLOAD_DIR default, design-doc.md §16.1)
/data/uploads/

# Downloaded local voice models (VOICE_MODEL_DIR default) — large binaries,
# re-downloaded on first use, never committed.
/data/models/
